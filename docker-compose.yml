version: '3.8'

services:
  postgres:
    image: postgres:18
    container_name: meetupmanagement-postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-user}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-password}
      - POSTGRES_DB=${POSTGRES_DB:-MEETUPMANAGEMENT}
    volumes:
      - ./postgres-data:/var/lib/postgresql